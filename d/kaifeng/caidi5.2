inherit ROOM;

int do_get2(string arg) 
{ 
        object me; 
        me=this_player(); 

        if(playerp(me) && !wizardp(me) && arg!="all" && arg!="head")
        { 
                        write("你想掏嘛？离开菜地再说吧！！\n");  
                        return 1; 
        } 

        return 0; 
} 

void create()
{
        set("short", "菜地");
        set("long", @LONG
这片菜园子几畦地里种了一些时鲜蔬菜。另外有几个木桩搭起的
菜架子，供一些爬藤的蔬菜生长，不过你举目看去，老大的一片菜园
子，居然疏疏落落的没有几棵菜。
LONG );
        set("no_clean_up", 0);
        set("exits", ([
                  "west" : __DIR__"caidi",
                  "east" : __DIR__"kongdi",
                  "south" : __DIR__"maofang",
                  "north" : __DIR__"jiaomen",
        ]));
        set("outdoors", "xiangguosi");

        set("coor/x", -4990);
        set("coor/y", 2210);
        set("coor/z", 10);
        setup();
}

void init() 
{ 
        add_action("do_get2", "get");  
}


